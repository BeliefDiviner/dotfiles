# Uncomment this for faster config changes.
# keybind = super+option+r=reload_config

# Basic preferences.
font-family = "MesloLGS NF"
font-size = 12
theme = GruvboxDark

# Fix window borders.
window-padding-x = 0
window-padding-y = 0
window-save-state = always

# Unbind clashing configs.
keybind = super+,=unbind
keybind = shift+super+,=unbind
